<div id="user">
<div id="errors"></div>
<%= form_for @user, remote: true, method: :post, url: '/users', html: { class: "login" } do |f| %>
  <%= f.text_field :email, :placeholder => "email" %>
  <% array = ["1", "2", "3", "4", "Teacher", "Alum"] %>
  <% (0..5).each do |i| %>
  	<span id="<%= array[i] %>-signup">
	  	<%= f.label array[i]%>
	  	<%= f.radio_button :phase, i %>
  	</span>
  <% end %>

  <%= f.submit "Log in / Sign up" %>

<% end %>
</div>
